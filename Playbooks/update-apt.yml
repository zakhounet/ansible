---
- name: Update Server
  hosts: all
  become: true
  tasks:
    - name: Update apt packages
      apt:
        upgrade: yes
        update_cache: yes
        cache_valid_time: 86400 #One day
      
      
      
      #ansible.builtin.apt:
      #  update_cache: true
